<div class="container">
    <div class="row">
        <div class="col-md-12">
        <h2>Categorías</h2>
          <%= link_to  "New Category", new_category_path, remote: true, class:"btn btn-success pull-left glyphicon glyphicon-plus" %>

            <table id="expenses-table" class="table table-hover" style="margin-top: 20px;">
                <thead>
                  <tr>
                    <th>Categoría</th>
                    <th class="text-right">Acciones</th>
                  </tr>
                </thead>
                <tbody>
                    <% @categories.each do |category| %>
                    <tr>
                    <td><%= category.name %></td>
                        <td class="text-right" style="width: 170px;">
                        <div class="actions">
                        <%= link_to "Edit", edit_category_path(category), remote: true, class:"glyphicon glyphicon-edit" %>
                        <%= link_to "Eliminar", category, method: :delete, data:{ confirm: "Desea Eliminar la Category"}, class:"glyphicon glyphicon-trash" %>
                        </div>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
            </table>
        </div>
    </div>
</div>